export interface BallInterface {
    x: number, 
    y: number, 
    deltaY: number,
    deltaX: number,
    size: number,
};

export interface RacketInterface {
    x: number, 
    y: number, 
    size: number,
    width: number
};